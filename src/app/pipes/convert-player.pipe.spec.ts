import { ConvertPlayerPipe } from './convert-player.pipe';

describe('ConvertPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
